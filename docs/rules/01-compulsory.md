# minicli compulsory （强制要求）

- 全项目优先级最高的agents行为约束规则，每一轮对话的最后必须显式判断该文件中的规则是否符合
- 1.所有基于该项目或可访问到该项目的对话 / agent，在收到一条指令可能涉及到文件修改时，绝对不能立刻做出文件修改。必须先生成文件修改的plan，plan必须包含详细的diff，人工在对话框确认后才可以执行文件改动
- 2.所有基于该项目或可访问到该项目的对话 / agent，强制扫描“每轮对话需要修改的文件清单”，里面包含每个记录文件的生成规则，文件位置在 docs/settling.md，没有的话则需要新建
- 